version: 0.2

env:
  secrets-manager:
    GITHUB_TOKEN: ct-docs-secrets:GITHUB_TOKEN
    GH_TOKEN: ct-docs-secrets:GH_TOKEN
  variables:
    MAVEN_OPTS: "-Xms4g -Xmx32g -XX:MetaspaceSize=2g -XX:MaxMetaspaceSize=16g -XX:+UseG1GC"

phases:
  install:
    runtime-versions:
      java: corretto23
      nodejs: 22
    commands:
      - java -version
      - mvn --version
      - node --version
      - npm --version

  pre_build:
    commands:
      - echo "=== Maven Publish Check (Dry Run) ==="
      - echo "Commit SHA  - ${CODEBUILD_RESOLVED_SOURCE_VERSION}"
      - echo "Branch/Ref  - ${GITHUB_REF_NAME}"

  build:
    commands:
      # 1. Generate Website (Compodoc) documentation (required by Crypto-Trader-Docs)
      - cd $CODEBUILD_SRC_DIR/Crypto-Trader-Website && npm install && npm run docs:generate && cd $CODEBUILD_SRC_DIR

      # 2. Dry-run deploy to a local filesystem repository (includes package)
      - |
        set -e
        export LOCAL_REPO=$(mktemp -d)
        echo "Deploying to local repo: ${LOCAL_REPO}"

        mvn -B -T 1C -DskipTests \
          -DaltDeploymentRepository=local-check::file://${LOCAL_REPO} \
          deploy --file pom.xml

        echo ""
        echo "=== Dry Run Results ==="
        echo "Artifacts that would be published:"
        find "${LOCAL_REPO}" -type f -name "*.pom" | while read pom; do
          MODULE_DIR=$(basename $(dirname $(dirname $pom)))
          POM_FILE=$(basename $pom)
          echo "  [OK] ${MODULE_DIR}/${POM_FILE}"
        done

        ARTIFACT_COUNT=$(find "${LOCAL_REPO}" -type f -name "*.pom" | wc -l)
        echo ""
        echo "Total artifacts validated: ${ARTIFACT_COUNT}"

      # 2. GAV consistency check from deployed POMs (no extra Maven calls)
      - |
        set -e
        echo ""
        echo "=== GAV Consistency Check ==="
        gav_errors=0

        for pom in $(find "${LOCAL_REPO}" -type f -name "*.pom"); do
          G=$(sed -n '/<parent>/,/<\/parent>/!{ s/.*<groupId>\(.*\)<\/groupId>.*/\1/p }' "$pom" | head -1)
          A=$(sed -n '/<parent>/,/<\/parent>/!{ s/.*<artifactId>\(.*\)<\/artifactId>.*/\1/p }' "$pom" | head -1)
          V=$(sed -n '/<parent>/,/<\/parent>/!{ s/.*<version>\(.*\)<\/version>.*/\1/p }' "$pom" | head -1)

          if [ -z "$G" ] || [ -z "$A" ] || [ -z "$V" ]; then
            echo "  [FAIL] INVALID GAV: $pom"
            gav_errors=$((gav_errors + 1))
          else
            echo "  [OK] $G:$A:$V"
          fi
        done

        if [ $gav_errors -gt 0 ]; then
          echo "ERROR: $gav_errors module(s) have invalid GAV coordinates."
          exit 1
        fi

        echo ""
        echo "All modules validated successfully."

  post_build:
    commands:
      # Teardown: clean up the local dry-run repo
      - |
        echo "=== Teardown ==="
        if [ -d "${LOCAL_REPO}" ]; then
          rm -rf "${LOCAL_REPO}" && echo "Cleaned local deploy repo." || true
        fi
        echo "Maven publish check complete."

cache:
  paths:
    - '/root/.m2/**/*'
