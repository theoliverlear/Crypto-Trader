package org.cryptotrader.api.library.events;

/**
 * Marker class to ensure the package is recognized as non-empty when compiling module-info.java.
 * This class has no runtime use and can be removed if Kotlin sources are compiled before module-info.
 */
public final class PackageMarker {
    private PackageMarker() {
        // no instances
    }
}
