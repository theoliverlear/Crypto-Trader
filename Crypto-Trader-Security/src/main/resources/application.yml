server:
  port: 8095
spring:
  application:
    name: crypto-trader-security
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  config:
    import: aws-secretsmanager:/cryptotrader/dev
  cloud:
    aws:
      region:
        static: us-east-1